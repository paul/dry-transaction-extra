# frozen_string_literal: true

RSpec.describe Dry::Transaction::Extra do
  it "has a version number" do
    expect(Dry::Transaction::Extra::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
